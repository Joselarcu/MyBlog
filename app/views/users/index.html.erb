<h1>Users</h1>

<% @users.each do |user| %>
  <%= link_to user.username, user_path(user) %> 
  <%= link_to 'Edit',  edit_user_path(user) %>
  <%= link_to 'Delete', user, method: :delete, data: { confirm: 'are you sure?' }, remote: true %>
  <br>

<% end %>



